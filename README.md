# SDIP - Smart Delivery Insight Platform

ระบบตรวจสอบงานค้างสำหรับไปรษณีย์ไทย เพื่อป้องกันการตก SLA

## 🎯 วัตถุประสงค์

- ตรวจสอบงานค้าง (Backlog) แยกตามประเภท R / EMS / COD
- ตรวจสอบงานคืน (Return) แยกตามประเภท R / EMS / COD
- แสดงสถิติงานของพนักงานแต่ละคน
- รองรับระดับผู้ใช้งาน 3 ระดับ: User / Admin / PowerUser

## 📊 ข้อมูลอ้างอิง

**Google Sheets:** https://docs.google.com/spreadsheets/d/1qlpxCNo7kefla-dZCdx9Jr70e-x-9rVNkpau6QmPeUk/edit

**Spreadsheet ID:** `1qlpxCNo7kefla-dZCdx9Jr70e-x-9rVNkpau6QmPeUk`

## 🏗️ โครงสร้างโปรเจกต์

```
SDIP_by_Surat/
├── appsscript.json        # Config file
├── Code.gs                # ไฟล์หลัก (Entry point + Test functions)
├── Config.gs              # การตั้งค่า Spreadsheet ID และ Column mapping
├── Auth.gs                # ระบบ Login & Authentication
├── DataService.gs         # จัดการข้อมูลจาก Google Sheets
├── WebApp.gs              # Web App handlers (doGet + server functions)
├── Index.html             # หน้าหลัก (Single Page: Login + Dashboard)
├── ExportData.gs          # สคริปต์ช่วยเหลือสำหรับ export ข้อมูล
└── README.md              # คู่มือโปรเจกต์
```

## 📂 โครงสร้างชีตใน Google Sheets

### ชีตที่ใช้งาน

1. **SDIPEmployee** - ข้อมูลพนักงาน (รวม Login)
   - Username, Password, Useraccesslevel
   - Name, Position, Paymentside (โซน)
   - ข้อมูลติดต่อ (เบอร์โทร, LINE, Email)

2. **งานค้าง (Backlog)** - 3 ชีต
   - SDIPBacklogR - งานค้าง R (ลงทะเบียน)
   - SDIPBacklogEMS - งานค้าง EMS
   - SDIPBacklogCOD - งานค้าง COD

3. **งานคืน (Return)** - 3 ชีต
   - SDIPReturnedR - งานคืน R
   - SDIPReturnedEMS - งานคืน EMS
   - SDIPReturnedCOD - งานคืน COD

**โครงสร้างคอลัมน์ในชีตงาน (15 คอลัมน์):**
- หมายเลขสิ่งของ (Barcode)
- ผู้ดำเนินการ (Operator)
- ชื่อ-ที่อยู่ผู้รับ
- วันที่ สแกนล่าสุด
- สาเหตุ, COD, Lazada
- จำนวนวันถือครอง
- จำนวนครั้งที่พยายามนำจ่าย
- สถานะต่างๆ (ส่งมอบ, นัดรับ, ติดต่อผู้รับ, เตรียมคืน)

## 🔐 ระดับผู้ใช้งาน

| ระดับ | สิทธิ์ | คำอธิบาย |
|-------|--------|----------|
| **User** | ดูข้อมูลตัวเอง | พนักงานทั่วไป - เห็นเฉพาะงานของตัวเอง |
| **PowerUser** | ดูข้อมูลทั้งหมด | ผู้ควบคุม - เห็นข้อมูลทุกคน |
| **Admin** | ดูข้อมูลทั้งหมด + จัดการระบบ | ผู้ดูแลระบบ - สิทธิ์เต็ม |

## ✨ ฟีเจอร์หลัก

### 1. งานค้าง (Backlog Tab)
- แสดงจำนวนงานค้างทั้งหมด
- แยกตามประเภท: R / EMS / COD
- สรุปรวมทั้งหมด

### 2. งานคืน (Return Tab)
- แสดงจำนวนงานคืนทั้งหมด
- แยกตามประเภท: R / EMS / COD
- สรุปรวมทั้งหมด

### 3. Sidebar รายชื่อพนักงาน
- แสดงรายชื่อพนักงานทั้งหมด
- แสดงสถิติงานของแต่ละคน (ค้าง/คืน)
- สีเตือนตามจำนวนงาน:
  - 🟢 เขียว: งานน้อย (< 15)
  - 🟠 ส้ม: งานปานกลาง (15-29)
  - 🔴 แดง: งานมาก (≥ 30)
- คลิกเพื่อดูรายละเอียดเฉพาะคน

### 4. ระบบ Login
- Login ด้วย Username และ Password
- ดึงข้อมูลจากชีต SDIPEmployee
- Single Page Application (ไม่มีการ redirect ระหว่างหน้า)

## 🚀 วิธีการติดตั้ง

### ขั้นตอนที่ 1: เตรียม Google Sheets

1. เปิด Google Sheets: https://docs.google.com/spreadsheets/d/1qlpxCNo7kefla-dZCdx9Jr70e-x-9rVNkpau6QmPeUk/edit

2. ตรวจสอบว่ามีชีตครบทั้งหมด 7 ชีต:
   - SDIPEmployee
   - SDIPBacklogR, SDIPBacklogEMS, SDIPBacklogCOD
   - SDIPReturnedR, SDIPReturnedEMS, SDIPReturnedCOD

### ขั้นตอนที่ 2: Copy โค้ดไปใน Apps Script

1. ไปที่ **Extensions > Apps Script**

2. สร้างไฟล์ทั้งหมดจากโปรเจกต์นี้:

**ไฟล์ .gs:**
- `Code.gs` - ไฟล์หลัก
- `Config.gs` - การตั้งค่า
- `Auth.gs` - ระบบ Login
- `DataService.gs` - จัดการข้อมูล
- `WebApp.gs` - Web App handlers
- `ExportData.gs` - (Optional) ช่วยเหลือ export ข้อมูล

**ไฟล์ .html:**
- `Index.html` - หน้าหลัก (รวม Login + Dashboard)

3. คลิก **Save project** (💾)

### ขั้นตอนที่ 3: ทดสอบระบบ

1. รันฟังก์ชัน **`testConnection()`** เพื่อตรวจสอบการเชื่อมต่อ

2. รันฟังก์ชัน **`testGetData()`** เพื่อทดสอบดึงข้อมูล

3. ดู Logs: **View > Logs** (Ctrl+Enter)

### ขั้นตอนที่ 4: Deploy Web App

1. คลิก **Deploy > New deployment**

2. คลิก **Select type** → เลือก **Web app**

3. ตั้งค่า:
   - **Description:** `SDIP v2.0`
   - **Execute as:** `Me (your@email.com)`
   - **Who has access:** `Anyone`

4. คลิก **Deploy**

5. **Copy Web App URL**

### ขั้นตอนที่ 5: เข้าใช้งาน

1. เปิด Web App URL
2. Login ด้วย Username และ Password จากชีต SDIPEmployee
3. เริ่มใช้งาน Dashboard

## 🧪 การทดสอบ

### ฟังก์ชันทดสอบใน Code.gs

```javascript
testConnection()        // ทดสอบการเชื่อมต่อชีต
testGetData()          // ทดสอบดึงข้อมูล
testLogin()            // ทดสอบ Login
testSearchBarcode()    // ทดสอบค้นหา Barcode
showTotalStats()       // แสดงสถิติรวม
```

### เมนู Custom ใน Google Sheets

หลังจาก Deploy แล้ว จะมีเมนู **🚀 SDIP** ใน Google Sheets:
- 📊 แสดงสถิติรวม
- 🧪 ทดสอบการเชื่อมต่อ
- 🧪 ทดสอบดึงข้อมูล
- ℹ️ เกี่ยวกับ SDIP

## 📝 หมายเหตุสำหรับ Developer

### การปรับแต่ง Spreadsheet ID

แก้ไขในไฟล์ `Config.gs`:

```javascript
const SPREADSHEET_ID = 'YOUR_SPREADSHEET_ID';
```

### การปรับแต่ง Column Mapping

หากโครงสร้างคอลัมน์เปลี่ยน แก้ไขใน `Config.gs`:

```javascript
const EMPLOYEE_COLUMNS = { ... }
const WORK_COLUMNS = { ... }
```

### การเพิ่มฟีเจอร์ใหม่

1. เพิ่มฟังก์ชันใน `DataService.gs`
2. เพิ่ม server function ใน `WebApp.gs`
3. เรียกใช้จาก client-side ผ่าน `google.script.run`

## 🔧 การ Troubleshooting

### ปัญหา: ไม่พบชีต

**แก้ไข:** ตรวจสอบชื่อชีตใน Google Sheets ว่าตรงกับ `SHEET_NAMES` ใน Config.gs

### ปัญหา: Login ไม่ได้

**แก้ไข:**
1. ตรวจสอบ Username และ Password ในชีต SDIPEmployee
2. รันฟังก์ชัน `testLogin()` เพื่อดู error

### ปัญหา: ไม่แสดงข้อมูล

**แก้ไข:**
1. รันฟังก์ชัน `testGetData()` ใน Apps Script
2. ดู Logs (View > Logs)
3. ตรวจสอบ Column mapping ใน Config.gs

## 📞 การติดต่อ

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Logs ใน Apps Script (View > Logs)
2. รันฟังก์ชันทดสอบต่างๆ
3. ตรวจสอบ Console ในเบราว์เซอร์ (F12)

## 📜 License

Developed by Claude AI for Thailand Post
Version 2.0.0 | November 2025

---

**สำคัญ:** ระบบนี้ใช้ข้อมูลจริงจาก Google Sheets ของไปรษณีย์ไทย กรุณาตรวจสอบสิทธิ์การเข้าถึงและความปลอดภัยของข้อมูล
